[build-system]
requires=["setuptools>=69","wheel"]
build-backend="setuptools.build_meta"
[project]
name="parametric-cad-evidence-pipeline"
version="0.1.0"
description="Reproducible manifests and verification for parametric CAD artifacts"
readme="README.md"
requires-python=">=3.11"
license={text="Apache-2.0"}
authors=[{name="Adityavardhan Mishra"}]
dependencies=[]
[tool.setuptools.packages.find]
where=["src"]
